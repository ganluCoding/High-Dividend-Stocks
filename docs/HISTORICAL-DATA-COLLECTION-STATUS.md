# 历史行情回填状态（2026-08-10）

## 本次发布

运行时已发布版本：`release_price_history_20260810_1415`。

| 指标 | 结果 |
|---|---:|
| 历史日线记录 | 约 2,924,940 条 |
| 有历史价格的标的 | 1,716 个（1,658 只股票、58 只 ETF） |
| 交易日范围 | 2019-01-02 至 2026-08-07 |
| 股票全市场中至少约 5 年交易日 | 1,545/5,422（28.5%） |
| ETF 中至少约 5 年交易日 | 6/58 |
| 历史行情质量门禁 | 通过；重复键与价格异常未发现 |

同一发布版本的低频事实覆盖为：股票分红事件 4,913/5,422（90.6%），股票财务观察值 5,201/5,422（95.9%），ETF 分配事件 4/1,603（0.2%），ETF 产品事实 0/1,603。

本次回填由前四个批次和本次全市场扩充批次组成：

- 股票首批：100 只、184,300 条；
- ETF 首批：10 只、5,856 条；
- 股票第二批：100 只、184,300 条；
- ETF 第二批：50 只、2,451 条（去重后写入 2,121 条）。
- 股票扩充批次：500 只中 499 只成功，写入 797,849 条；1 只因 BaoStock 会话失败进入失败清单。
- 分红阳性定向批次：500/500 成功，采集 837,940 条，去重后写入 788,804 条。
- 分红阳性定向批次（二）：500/500 成功，采集并新增 890,365 条。

所有批次都先把原始 CSV、请求参数、采集时间和 SHA-256 写入运行目录；成功率达到 80% 才能写入源库并生成发布版本。失败批次进入隔离目录，不修改正式库。

## 自动更新

已安装用户级 LaunchAgent：`com.highdividend.historical-backfill`。

它每天低频运行一次，每次最多处理 500 只股票和 100 只 ETF；股票优先从已有分红事件但历史日数不足约 1,000 个交易日的标的中选择，继续填补潜在高股息候选。成功批次会自动发布新版本；失败只保留日志和隔离结果。运行日志：

`~/Library/Application Support/HighDividend/logs/historical-backfill.out.log`

手动运行或重新安装：

```bash
python3 scripts/autonomous_history_backfill.py \
  --runtime-root "$HOME/Library/Application Support/HighDividend"
python3 scripts/install_historical_backfill_agent.py --install
```

## 当前边界与回测门槛

“有历史价格”不等于“已经具备可信回测”。当前历史价格覆盖为 1,716/7,025，其中达到五年门槛的股票为 1,545/5,422。下一阶段必须先将股票五年历史覆盖提升到至少 80%（或分红阳性股票的 80%，取较严者），并保留分红、财务的公告时点；ETF 回测另需产品事实与分配事件。未达到门槛前，不发布“全市场回测收益率”，只能发布数据诊断或明确标注范围的示例回测。

BaoStock 与新浪接口目前作为本机内部研究的便利采集层；自动抓取、保存和再分发条款仍需单独核验，因此不对外分发原始响应。
